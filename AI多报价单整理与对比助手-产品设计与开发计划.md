# AI 多报价单整理与对比助手

> 产品设计、竞品策略与开发实施计划  
> 暂定产品名：报价齐  
> 文档版本：V1.0  
> 更新日期：2026-07-26

---

## 1. 项目摘要

### 1.1 产品定义

“报价齐”是一款面向中小企业采购、行政、项目负责人和专业自由职业者的 AI 文档工具。

用户上传 2～20 份格式不同的报价文件，系统自动完成：

1. 识别供应商、商品、规格、数量、价格和商业条款；
2. 将不同供应商中名称不同但实际可比的项目自动对齐；
3. 统一数量、单位、税费、运费和币种口径；
4. 标记缺失项、计算异常和匹配冲突；
5. 生成带原文证据的横向对比表；
6. 导出可继续编辑的 Excel、PDF 或分享链接。

核心承诺：

> AI 负责整理，人负责确认和决策。每个关键数字都可以追溯到原报价文件。

### 1.2 核心价值

产品将传统流程：

```text
逐份打开文件
→ 复制到 Excel
→ 手工统一商品名称
→ 核对数量和价格
→ 处理税费与运费
→ 制作比较表
```

压缩为：

```text
上传报价
→ 复核识别结果
→ 处理少量匹配冲突
→ 导出比较表
```

### 1.3 立项结论

项目适合以“小工具切入、专业工作流变现”的方式推进。

推荐定位：

```text
免费 SEO 工具
    +
按次处理
    +
个人专业版
    +
轻量团队版
```

不建议：

- 做成通用 AI 聊天工具；
- 一开始开发完整采购 ERP；
- 自动替用户选择供应商；
- 第一阶段进入大型招投标、政府采购或复杂工程报价。

---

## 2. 目标用户

### 2.1 核心用户

5～100 人企业中的采购、行政或项目负责人。

典型特征：

- 每月进行 2～20 次询价；
- 每次收到 2～10 份报价；
- 报价主要来自微信、邮件和供应商销售人员；
- 文件格式以 PDF、Excel、Word、图片为主；
- 当前使用 Excel 手工整理；
- 没有专职采购系统或 IT 团队；
- 对数字错误非常敏感；
- 不愿部署完整采购系统。

### 2.2 次级用户

- 装修业主；
- 婚庆及活动策划；
- 广告、设计、摄影工作室；
- 电商卖家；
- 学校及培训机构行政；
- 自由职业项目经理；
- 小型工程和服务团队。

这些用户使用频率较低，更适合按次付费。

### 2.3 企业用户

可能购买团队版或 API 的用户：

- 多人采购团队；
- 连锁门店总部；
- 代理商和经销商；
- 需要定期汇总供应商报价的小型制造企业；
- 需要将比价能力嵌入自身系统的软件服务商。

### 2.4 暂不服务的用户

MVP 阶段不服务：

- 政府采购；
- 医疗采购；
- 大型建筑工程招标；
- 金融机构；
- 需要完整审计流程的集团企业；
- 需要 ERP、库存、付款和审批一体化的组织。

---

## 3. 用户问题与使用场景

### 3.1 核心问题

用户目前普遍存在以下问题：

- 报价文件格式不统一；
- 商品名称、规格和单位写法不同；
- 扫描件和图片无法直接复制；
- 含税、未税、运费口径不一致；
- Excel 手工整理耗时；
- 容易复制错数字；
- 无法快速确认某个数字来自哪里；
- 不同员工制作的对比表格式不同；
- 历史匹配规则无法复用。

### 3.2 典型场景

#### 场景 A：办公用品采购

三家供应商分别使用不同名称描述同一种打印纸。系统自动识别品牌、克重、尺寸和包装数量，统一为可比较的每箱价格。

#### 场景 B：设备采购

系统发现两个报价型号相似，但内存、硬盘或附件配置不同，将其标记为“可能匹配，需要确认”，而不是直接比较价格。

#### 场景 C：服务报价

不同供应商分别采用“项目总包”“服务费＋材料费”“按人天”计价。系统保留原报价结构，提示用户当前项目不能直接按单价比较。

#### 场景 D：装修或活动策划

用户上传多份图片或 PDF 报价，系统生成一次性比较结果。用户不需要订阅，通过单次付费获得 Excel 和 PDF。

---

## 4. 竞品分析与产品继承策略

> 以下分析基于竞品公开页面，不代表完整付费实测。

### 4.1 PriceMirror AI

公开能力：

- PDF、Excel、Word 和扫描件 OCR；
- 行项目识别；
- 跨报价语义匹配；
- 保存项目；
- 导出带真实公式的 Excel；
- PDF 导出；
- 面向欧洲企业的数据保护能力；
- Basic、Pro、Power 和 Enterprise 分层定价。

应继承：

- 语义匹配不同行项目；
- Excel 中保留真实公式；
- 保存项目后继续编辑；
- 聚焦报价对比，而非通用文档问答。

公开页面未重点解决或未明显强调：

- 中文报价和本地单位；
- 每个结果单元格的原文证据；
- 低频用户按次付费；
- 中国常见税费、运费和报价表达。

本产品的补偿方案：

- 深度支持中文扫描件、中文单位和合并单元格；
- 每个关键字段绑定页码、坐标和原文；
- 同时提供免费、按次和订阅；
- 内置含税、未税、运费和人民币格式处理。

### 4.2 Buyer24

公开能力：

- 免费工具无需注册；
- 支持 PDF、Excel、Word 和图片；
- 自动提取价格、数量、交期和供应商；
- 生成横向比较；
- 导出 Excel 和 PDF；
- 访客数据到期删除；
- 完整产品覆盖 RFQ 和采购流程。

应继承：

- 无需注册即可体验；
- 多种文件格式；
- 免费工具作为获客入口；
- 明确的数据删除策略；
- 报价比较后直接导出。

不足及风险：

- 完整产品逐渐成为较重的采购系统；
- 免费版文件数有限；
- 下载需要验证邮箱；
- AI 推荐供应商存在可信度和责任风险；
- 对低频用户而言订阅门槛较高。

本产品的补偿方案：

- 不进入供应商网络、采购订单和完整 RFQ；
- 免费结果无需邮箱即可查看；
- 提供单次项目付费；
- MVP 不自动推荐供应商；
- 只展示事实、异常、缺失和确定性计算。

### 4.3 PDFExcel

公开能力：

- 无需预设供应商模板；
- 用户自定义需要提取的字段；
- 支持 PDF、扫描件和照片；
- 导出 Excel、CSV 等格式；
- 强调加密、处理后删除和不用于模型训练。

应继承：

- 用户自定义字段；
- 无模板抽取；
- 兼容扫描件；
- 隐私说明清晰；
- 输出进入用户熟悉的表格工作流。

不足：

- 核心是单文档数据抽取；
- 不负责跨供应商匹配；
- 不处理报价可比性；
- 不形成供应商横向比较。

本产品的补偿方案：

- 增加跨文档匹配层；
- 增加税费、运费和单位标准化；
- 支持人工修改匹配组；
- 保存企业自己的字段模板和匹配规则。

### 4.4 Invoice Scraper

公开能力：

- 混合供应商批量处理；
- 行项目抽取；
- 每字段置信度；
- Excel 导出；
- 免费页数；
- 按量购买且额度不过期。

应继承：

- 字段级置信度；
- 混合版式批量处理；
- 按量付费；
- 结构化行项目输出。

不足：

- 面向发票而非报价；
- 不负责多报价项目匹配；
- 不比较交期、付款和质保；
- 置信度不能替代原文证据。

本产品的补偿方案：

- 置信度与原文证据同时提供；
- 区分原文值、计算值和用户确认值；
- 商业条款进入同一对比结果；
- 单独设计匹配冲突复核队列。

### 4.5 竞争定位

```text
通用 OCR/PDF 工具          报价齐              完整采购系统
-----------------        ---------           -------------
只抽取数据                抽取＋匹配＋比较      询价＋比价＋审批
缺少业务理解              轻量、可复核          实施较重
低价或免费                按次/订阅             企业销售
```

产品不能变成“更小的采购 ERP”，而应成为“更可靠的报价比较工作台”。

---

## 5. 产品原则

1. 先展示事实，再提供分析；
2. 关键金额必须可追溯；
3. 不确定的内容不得自动补全；
4. 缺失值不得按零计算；
5. 用户确认优先于模型判断；
6. 原始数据永不被覆盖；
7. 结果可以导出，不锁死在平台中；
8. AI 不负责基础算术；
9. MVP 不自动推荐供应商；
10. 不使用通用聊天框作为主界面。

---

## 6. 产品范围

### 6.1 MVP 必须包含

- PDF、XLSX、JPG、PNG 上传；
- 每个项目 2～5 份报价；
- 文件质量检查；
- 供应商和报价基础信息提取；
- 行项目提取；
- 字段置信度；
- 原文证据定位；
- 跨供应商行项目匹配；
- 匹配冲突复核；
- 简单单位标准化；
- 税费、运费状态展示；
- 确定性数学校验；
- 横向比较表；
- Excel 导出；
- 项目及文件删除；
- 基础使用统计和错误日志。

### 6.2 MVP 明确不做

- 供应商推荐；
- 自动发送询价；
- 采购订单；
- 支付和发票；
- 库存；
- 审批流；
- 合同法律审查；
- 复杂工程量清单；
- 多币种实时汇率；
- 团队协作；
- API；
- 小程序；
- 私有部署。

---

## 7. 核心用户流程

```mermaid
flowchart LR
    A["上传 2～5 份报价"] --> B["文件质量检查"]
    B --> C["逐份抽取字段和行项目"]
    C --> D["第一次复核：原始数据"]
    D --> E["自动匹配同类项目"]
    E --> F["第二次复核：匹配冲突"]
    F --> G["统一单位、税费和运费口径"]
    G --> H["生成横向对比表"]
    H --> I["检查异常和缺失项"]
    I --> J["导出 Excel / PDF"]
```

### 7.1 第一次复核

确认 AI 是否正确读取每份报价：

- 供应商；
- 报价日期；
- 总价；
- 商品名称；
- 数量；
- 单位；
- 单价；
- 小计；
- 商业条款。

### 7.2 第二次复核

确认不同供应商的行项目是否应该放在同一组比较。

用户可以：

- 合并；
- 拆分；
- 拖动到其他比较组；
- 创建新组；
- 标记替代品；
- 标记赠品；
- 标记供应商独有费用。

---

## 8. 详细功能设计

### 8.1 首页与试用

首页核心入口：

> 拖入 2 份以上报价，生成可追溯对比表。

免费访客：

- 无需注册；
- 最多上传 3 份文件；
- 可以查看完整结果；
- 可以修正错误；
- 可以下载 CSV；
- 可以下载带品牌标识的 PDF；
- 24 小时后自动删除。

首页应提供脱敏示例项目，用户无需上传真实文件也能体验。

### 8.2 文件质量检查

上传后首先执行：

- 文件类型验证；
- 文件大小和页数限制；
- 文件哈希和重复检测；
- 密码文件检测；
- 空白文件检测；
- 图片清晰度检测；
- 图片倾斜检测；
- PDF 文本层检测；
- Excel 隐藏 Sheet 检测；
- 文件语言初步检测；
- 是否包含多份报价的检测。

质量不合格时优先要求重新上传，避免无效 AI 成本。

### 8.3 基础信息抽取

每份报价提取：

- 供应商名称；
- 联系人和联系方式；
- 报价编号；
- 报价日期；
- 有效期；
- 客户名称；
- 币种；
- 税率；
- 含税/未税；
- 总价；
- 运费；
- 折扣；
- 交货期；
- 付款条件；
- 质保；
- 备注。

字段状态：

| 状态 | 界面表现 |
|---|---|
| 高置信度 | 正常显示 |
| 需要确认 | 黄色标记 |
| 无法识别 | 留空并说明原因 |

点击字段时，右侧文件预览器高亮原文。

### 8.4 行项目抽取

标准字段：

- 原始序号；
- 原始项目名称；
- 规格型号；
- 品牌；
- 数量；
- 原始单位；
- 单价；
- 小计；
- 税率；
- 交期；
- 备注；
- 来源文件；
- 页码；
- 原文坐标。

内部必须保存：

- 原始值；
- 标准化值；
- AI 建议值；
- 用户最终确认值；
- 置信度；
- 修改人；
- 修改时间。

### 8.5 项目匹配

匹配优先级：

1. SKU 或型号完全一致；
2. 品牌＋规格一致；
3. 标准化名称一致；
4. 单位和数量辅助判断；
5. 语义相似度判断；
6. 用户历史规则。

匹配结果：

- 确定匹配；
- 可能匹配；
- 不应匹配；
- 供应商独有项目。

不能因为名称相似就忽略规格差异。

### 8.6 单位标准化

首批支持：

- 件、个、套、箱、盒；
- 千克、克、吨；
- 米、厘米、毫米；
- 平方米、立方米；
- 小时、天、人天；
- 用户自定义单位。

自动换算只能来自：

- 报价原文；
- 用户设置；
- 已确认规则。

AI 不得猜测“一箱有多少件”。

### 8.7 税费和运费

提供三种口径：

1. 原始报价；
2. 标准化报价；
3. 用户调整后总成本。

用户可以选择：

- 统一含税；
- 统一未税；
- 包含运费；
- 不包含运费；
- 统一单位。

未知运费应显示“未知”，不得按 0 元处理。

### 8.8 异常检测

确定性异常：

- 数量 × 单价不等于小计；
- 行项目之和不等于总价；
- 税率与税额不一致；
- 同供应商重复项目；
- 币种冲突；
- 报价过期；
- 数量或价格为负；
- Excel 公式值与显示值冲突。

AI 辅助异常：

- 型号相似但配置不同；
- 一家供应商缺少关键服务；
- 某项费用一家包含、另一家单列；
- 交期、付款、质保差异明显。

AI 异常必须显示理由和证据，不得自动修改原值。

### 8.9 横向比较表

示例：

| 项目 | 数量 | 供应商 A | 供应商 B | 供应商 C | 差异 |
|---|---:|---:|---:|---:|---|
| 商品 1 | 10 件 | ¥100 | ¥96 | ¥105 | B 最低 |
| 商品 2 | 5 套 | ¥800 | 缺失 | ¥790 | B 未报价 |
| 运费 |  | 包含 | ¥200 | 未说明 | 需确认 |
| 交期 |  | 7 天 | 5 天 | 10 天 | B 最快 |

颜色规则：

- 绿色：当前最低可比价格；
- 黄色：需要确认；
- 红色：计算异常或关键缺失；
- 灰色：不可比较。

绿色不能代表系统推荐中标。

### 8.10 导出

Excel 包含：

- 原始数据；
- 标准化数据；
- 横向比较；
- 异常和待确认；
- 真实计算公式；
- 原文件和页码；
- 用户修改记录。

PDF 包含：

- 项目摘要；
- 供应商摘要；
- 横向比较；
- 缺失和异常；
- 用户备注；
- AI 整理声明；
- 生成时间。

---

## 9. 网页与小程序分工

### 9.1 网页端

网页是核心产品，负责：

- 多文件上传；
- 文件预览；
- 字段复核；
- 行项目匹配；
- 大表格编辑；
- 模板设置；
- Excel/PDF 导出；
- 团队管理。

### 9.2 小程序端

小程序在第二阶段以后开发，只负责：

- 拍照上传；
- 从微信文件选择报价；
- 查看处理进度；
- 确认少量黄色异常；
- 查看简化结果；
- 分享给同事；
- 接收完成提醒。

复杂对比表不应强行放入小程序。

---

## 10. 推荐技术架构

> 以下为单人开发友好型参考架构，可根据现有技术栈替换。

### 10.1 前端

- Next.js；
- TypeScript；
- React；
- Tailwind CSS 或成熟组件库；
- PDF.js 文件预览；
- SheetJS 或类似库处理 Excel；
- TanStack Table 构建比较表；
- 服务端渲染 SEO 页面。

### 10.2 后端

- Python FastAPI；
- PostgreSQL；
- Redis；
- Celery、RQ 或其他后台任务队列；
- S3 兼容对象存储；
- 独立文档处理 Worker；
- SSE 或 WebSocket 推送处理进度。

### 10.3 文档处理

- PDF 文本层解析；
- OCR 服务；
- Excel 原始单元格、合并信息和公式读取；
- 图片预处理；
- 文档版面分析；
- JSON Schema 约束的结构化抽取。

### 10.4 AI 层

- 模型路由器；
- 文档分类；
- 字段抽取；
- 行项目语义匹配；
- 异常解释；
- 严格结构化输出；
- 模型和提示词版本记录；
- 可替换模型供应商。

### 10.5 运维

- Docker；
- CI/CD；
- 错误监控；
- 结构化日志；
- 任务耗时和模型成本监控；
- 数据删除任务；
- 定期备份；
- 安全审计日志。

---

## 11. AI 处理流水线

```text
原生文件解析
    ↓
需要时才调用 OCR
    ↓
文档版式恢复
    ↓
报价类型识别
    ↓
结构化字段抽取
    ↓
确定性数学校验
    ↓
行项目语义匹配
    ↓
单位和税费标准化
    ↓
置信度与异常计算
    ↓
人工复核
    ↓
生成对比表
```

原则：

- Excel 读取单元格，不截图识别；
- 有文本层的 PDF 优先解析文本；
- 扫描件才使用 OCR；
- LLM 负责理解，不负责算术；
- 金额统一由程序重新计算；
- 不确定内容必须标记；
- 模型必须可替换；
- 相同文件通过哈希缓存；
- 简单任务使用低成本模型；
- 复杂匹配再调用能力更强的模型。

---

## 12. 核心数据模型

### 12.1 ComparisonProject

- 项目 ID；
- 用户/团队；
- 项目名称；
- 状态；
- 默认币种；
- 税费口径；
- 保存期限；
- 创建时间；
- 完成时间。

### 12.2 QuoteDocument

- 文件 ID；
- 项目 ID；
- 原文件名；
- 文件类型；
- 文件哈希；
- 页数；
- 存储位置；
- 解析状态；
- OCR 状态；
- 删除时间。

### 12.3 Supplier

- 供应商 ID；
- 原始名称；
- 标准化名称；
- 联系信息；
- 历史别名。

### 12.4 LineItem

- 行项目 ID；
- 报价文件 ID；
- 原始序号；
- 原始名称；
- 标准化名称；
- 规格；
- 品牌；
- 数量；
- 单位；
- 单价；
- 小计；
- 税率；
- 置信度。

### 12.5 MatchGroup

- 比较组 ID；
- 标准名称；
- 标准规格；
- 包含的行项目；
- 匹配状态；
- 匹配理由；
- 用户确认状态。

### 12.6 EvidenceAnchor

- 字段 ID；
- 文件 ID；
- 页码；
- 坐标；
- 原文；
- OCR 置信度。

### 12.7 NormalizationRule

- 用户/团队；
- 规则类型；
- 原始值；
- 标准值；
- 换算关系；
- 生效范围；
- 是否人工确认。

### 12.8 AuditEvent

- 操作对象；
- 操作者；
- 原值；
- 新值；
- 操作类型；
- AI/人工来源；
- 时间；
- 模型和提示词版本。

---

## 13. API 初步设计

### 13.1 项目

```http
POST   /api/projects
GET    /api/projects/{id}
DELETE /api/projects/{id}
POST   /api/projects/{id}/process
GET    /api/projects/{id}/status
```

### 13.2 文件

```http
POST   /api/projects/{id}/files
GET    /api/files/{id}
DELETE /api/files/{id}
GET    /api/files/{id}/preview
```

### 13.3 抽取与复核

```http
GET    /api/projects/{id}/quotes
PATCH  /api/quotes/{id}/fields
PATCH  /api/line-items/{id}
GET    /api/fields/{id}/evidence
```

### 13.4 匹配

```http
GET    /api/projects/{id}/match-groups
POST   /api/match-groups
PATCH  /api/match-groups/{id}
POST   /api/match-groups/{id}/merge
POST   /api/match-groups/{id}/split
```

### 13.5 导出

```http
POST   /api/projects/{id}/exports/excel
POST   /api/projects/{id}/exports/pdf
GET    /api/exports/{id}
```

---

## 14. 收费设计

### 14.1 免费访客

- 每天 1 个项目；
- 最多 3 份文件；
- 最多 20 页；
- 完整查看结果；
- CSV 导出；
- 带品牌 PDF；
- 24 小时后删除。

### 14.2 单次项目

建议测试价：9.9～19.9 元。

- 最多 10 份文件；
- 最多 100 页；
- Excel/PDF 导出；
- 30 天保存；
- 无品牌标识。

### 14.3 个人专业版

建议测试价：

- 29 元/月；
- 199 元/年。

权益：

- 每月 20 个项目；
- 每项目最多 20 份文件；
- 保存自定义模板；
- 历史匹配规则；
- 无品牌导出；
- 更长保存期限。

### 14.4 团队版

建议测试价：

- 99 元/月；
- 999～1999 元/年。

权益：

- 5 名成员；
- 团队模板；
- 项目共享；
- 评论；
- 操作日志；
- 统一供应商和单位规则；
- 权限设置。

### 14.5 企业版

建议 8000 元/年起：

- API；
- SSO；
- 企业品牌；
- 自定义保存期限；
- 独立字段模板；
- 批量任务；
- 专属支持。

MVP 不承诺私有部署。

---

## 15. SEO 页面规划

首批页面：

- `/compare-quotes`：多份报价单自动对比；
- `/pdf-quote-to-excel`：PDF 报价单转 Excel；
- `/image-quote-ocr`：图片报价单识别；
- `/supplier-comparison-template`：供应商报价比较表；
- `/tax-price-normalizer`：含税与未税价格换算；
- `/unit-price-normalizer`：单位价格换算。

每个页面必须包含：

- 真实可操作工具；
- 无需注册的示例；
- 输入和输出样例；
- 常见错误；
- 隐私说明；
- 适用范围；
- 相关工具内链。

禁止：

- 按城市生成页面；
- 按行业批量生成近似页面；
- 只替换关键词的 AI 内容；
- 没有实际工具的空洞落地页。

---

# 16. 开发步骤拆解

## 16.1 总体实施顺序

建议按照以下顺序推进：

```text
用户验证
→ 样本集与质量标准
→ 基础工程
→ 文件上传和解析
→ 字段抽取和证据
→ 行项目匹配
→ 复核界面
→ 比较和导出
→ 隐私与计费
→ SEO 页面
→ 封闭测试
→ 公开 MVP
```

预计单人完成可公开测试的 MVP：10～12 周。

---

## 16.2 阶段 0：需求与样本验证

周期：1～2 周。

### 任务 D0-01：用户访谈

目标：

- 访谈至少 10 位目标用户；
- 收集当前使用的报价比较表结构；
- 明确最常用的字段；
- 确认是否存在重复使用。

访谈问题：

- 最近一次比价用了多久？
- 收到了哪些文件格式？
- 哪些字段最容易出错？
- 最终需要把结果发给谁？
- 当前 Excel 模板有哪些列？
- 什么错误会造成实际损失？
- 愿意为一次处理或年度会员支付多少？

验收：

- 至少 6 位用户每月发生一次以上报价比较；
- 至少 5 位愿意试用；
- 至少 3 位愿意提供脱敏样本。

### 任务 D0-02：建立样本集

目标：建立至少 50 份脱敏报价样本。

覆盖：

- 原生 PDF；
- 扫描 PDF；
- Excel；
- 手机照片；
- 中文和少量中英混合；
- 不同税费、运费和单位；
- 合并单元格；
- 多页表格；
- 同商品不同命名；
- 相似型号但规格不同。

验收：

- 样本可以合法用于内部测试；
- 每份样本具备人工标注的正确结果；
- 关键字段形成标准答案。

### 任务 D0-03：定义质量指标

需要确定：

- 关键金额字段准确率；
- 行项目召回率；
- 行项目匹配准确率；
- 低置信度召回率；
- 未提示严重错误数量；
- 单项目处理时间。

验收：

- 形成可自动运行的基准测试数据；
- 后续每次更换模型都能回归测试。

---

## 16.3 阶段 1：基础工程

周期：1 周。

### 任务 D1-01：仓库与环境

- 前后端项目初始化；
- 环境变量管理；
- Docker 开发环境；
- 数据库迁移；
- 代码格式和静态检查；
- 单元测试框架；
- CI。

验收：

- 新环境可一条命令启动；
- 提交代码自动运行检查和测试。

### 任务 D1-02：数据库与对象存储

- PostgreSQL；
- 文件元数据表；
- 项目表；
- 对象存储；
- 文件签名上传 URL；
- 文件生命周期规则。

验收：

- 文件不经过 Web 服务内存中转即可安全上传；
- 可以按项目彻底删除文件和元数据。

### 任务 D1-03：后台任务队列

- 文档处理任务；
- 任务状态；
- 重试策略；
- 超时；
- 幂等；
- 失败队列；
- 进度事件。

验收：

- 重复提交不会重复收费或重复生成数据；
- Worker 异常后任务可恢复。

---

## 16.4 阶段 2：上传与文件解析

周期：1～2 周。

### 任务 D2-01：上传界面

- 拖拽上传；
- 文件选择；
- 上传进度；
- 格式和大小验证；
- 多文件列表；
- 删除与重新上传。

验收：

- 支持 PDF、XLSX、JPG、PNG；
- 至少可以同时上传 5 份文件；
- 错误提示明确。

### 任务 D2-02：文件质量检查

- 文件哈希；
- 重复文件；
- 密码 PDF；
- 空白文件；
- 图片分辨率；
- 倾斜和模糊；
- PDF 文本层；
- Excel Sheet 枚举。

验收：

- 无法处理的文件在调用 AI 前被阻止；
- 质量问题提供解决建议。

### 任务 D2-03：原生解析

- PDF 文本和坐标；
- Excel 单元格、公式、合并信息；
- 图片元数据；
- 页码和版面元素。

验收：

- 原生 Excel 数字不经过 OCR；
- PDF 文本可关联页码和坐标。

### 任务 D2-04：OCR 接入

- 扫描件识别；
- 图片预处理；
- OCR 坐标；
- OCR 置信度；
- 多页并行。

验收：

- OCR 输出可以在原图上高亮；
- 低清晰度页面被正确标记。

---

## 16.5 阶段 3：结构化抽取与证据系统

周期：2 周。

### 任务 D3-01：报价分类

区分：

- 商品报价；
- 服务报价；
- 混合报价；
- 无法识别。

验收：

- 无法识别的文件不会强行套用错误字段。

### 任务 D3-02：基础字段抽取

- JSON Schema；
- 供应商；
- 日期；
- 币种；
- 税费；
- 运费；
- 总价；
- 付款；
- 交期；
- 质保。

验收：

- 输出始终符合 Schema；
- 无法确定的字段为 `null`，不得猜测。

### 任务 D3-03：行项目抽取

- 表头识别；
- 多页表格合并；
- 行项目；
- 小计；
- 备注；
- 跨页表头；
- 合并单元格处理。

验收：

- 每一行保留来源文件和页码；
- 关键金额字段通过程序校验。

### 任务 D3-04：证据定位

- 字段与原文坐标绑定；
- 右侧文件预览；
- 点击字段高亮；
- OCR 和原生文本统一证据格式。

验收：

- 用户点击任一金额，2 秒内看到原文位置；
- 证据不存在时字段必须显示警告。

### 任务 D3-05：置信度系统

置信度综合：

- OCR 置信度；
- 结构化输出置信度；
- 数学一致性；
- 多处文本一致性；
- 字段格式规则。

验收：

- 低置信度字段进入复核队列；
- 置信度不能仅由 LLM 自报。

---

## 16.6 阶段 4：匹配、标准化与异常

周期：2 周。

### 任务 D4-01：名称标准化

- 大小写；
- 全半角；
- 标点；
- 常见单位；
- 品牌和型号拆分；
- 无意义修饰词；
- 用户别名。

验收：

- 原始名称保留；
- 标准化过程可解释。

### 任务 D4-02：匹配引擎

- 精确匹配；
- 规则匹配；
- 向量相似度；
- LLM 语义判断；
- 规格冲突检测；
- 匹配理由。

验收：

- 相似型号但关键规格不同的项目不会静默合并；
- 匹配结果分为确定、可能、不匹配。

### 任务 D4-03：匹配冲突界面

- 合并；
- 拆分；
- 拖动；
- 新建比较组；
- 标记替代品；
- 保存规则。

验收：

- 用户无需重新上传即可修正匹配；
- 修正规则可在同一项目后续重新应用。

### 任务 D4-04：单位标准化

- 基础物理单位；
- 包装单位；
- 自定义换算；
- 换算来源；
- 禁止猜测。

验收：

- 没有明确关系时保持不可比较；
- 用户可以输入并确认换算规则。

### 任务 D4-05：确定性异常检测

- 数量 × 单价；
- 小计和总价；
- 税额；
- 币种；
- 重复项目；
- 负数；
- 报价有效期。

验收：

- 数学错误由代码识别，不调用 LLM；
- 异常能定位到具体文件和行。

---

## 16.7 阶段 5：复核、比较与导出

周期：2 周。

### 任务 D5-01：第一次复核界面

- 左侧报价列表；
- 中部结构化数据；
- 右侧原文预览；
- 黄色待确认列表；
- 批量确认。

验收：

- 用户可以完成字段修改；
- 修改保留审计记录；
- 修改后自动重新计算。

### 任务 D5-02：横向比较表

- 固定项目列；
- 供应商列；
- 缺失提示；
- 最低价格；
- 条款比较；
- 排序和筛选；
- 列宽和冻结。

验收：

- 100 行、5 个供应商仍可流畅操作；
- 缺失值不会参与最低价计算。

### 任务 D5-03：价格口径

- 原始；
- 含税；
- 未税；
- 加运费；
- 自定义调整；
- 计算说明。

验收：

- 每个派生值都有公式；
- 未知值不会默认按零处理。

### 任务 D5-04：Excel 导出

- 原始数据 Sheet；
- 标准化数据 Sheet；
- 比较 Sheet；
- 异常 Sheet；
- 真实公式；
- 来源页码；
- 用户备注。

验收：

- Excel 打开无修复提示；
- 修改数量或单价后总价公式自动更新；
- 中文文件名和 Sheet 名正常。

### 任务 D5-05：PDF 导出

- 项目摘要；
- 比较表；
- 异常；
- 备注；
- AI 声明；
- 分页和打印。

验收：

- A4 打印布局可用；
- 大表格能够横向分页或缩放。

---

## 16.8 阶段 6：隐私、账户与商业化

周期：1～2 周。

### 任务 D6-01：访客模式

- 匿名项目；
- 24 小时删除；
- 无需邮箱查看结果；
- 防滥用限流。

验收：

- 访客数据按期自动删除；
- 删除过程有内部日志。

### 任务 D6-02：账户系统

- 注册登录；
- 项目列表；
- 保存期限；
- 用户删除；
- 账号注销。

验收：

- 用户可以删除全部项目和文件；
- 注销流程满足隐私要求。

### 任务 D6-03：计费

- 免费额度；
- 单次项目；
- 会员；
- 用量记录；
- 支付回调；
- 退款和失败处理。

验收：

- 支付回调幂等；
- 失败不会扣除额度；
- 用量解释清楚。

### 任务 D6-04：隐私和合规

- 隐私政策；
- 用户协议；
- 模型使用说明；
- 不用于训练声明；
- 文件保存期限；
- AI 内容标识；
- 已备案模型信息；
- 投诉和删除入口。

验收：

- 上线前完成法律和平台规则核对；
- 导出内容按适用要求添加标识。

---

## 16.9 阶段 7：SEO 和公开测试

周期：1 周。

### 任务 D7-01：核心 SEO 页面

- 多报价单比较；
- PDF 报价转 Excel；
- 图片报价识别；
- 供应商比较表模板；
- 含税未税换算。

验收：

- 页面可服务端渲染；
- 页面包含真实工具或示例；
- 标题、描述、Canonical、Sitemap 正确。

### 任务 D7-02：示例项目

- 办公用品；
- 电脑设备；
- 活动服务；
- 装修材料。

验收：

- 示例文件已脱敏；
- 用户可完整体验证据、匹配和导出。

### 任务 D7-03：分析和监控

跟踪：

- 上传开始率；
- 文件处理成功率；
- 第一次复核完成率；
- 匹配复核完成率；
- 导出率；
- 处理耗时；
- 每项目 AI 成本；
- 30 天重复使用；
- 付费转化。

验收：

- 不记录报价正文到第三方分析工具；
- 敏感字段不进入前端埋点。

---

## 17. 推荐迭代排期

| 周次 | 里程碑 | 主要输出 |
|---|---|---|
| 第 1～2 周 | 用户与样本验证 | 访谈、样本集、标准答案 |
| 第 3 周 | 基础工程 | 前后端、数据库、存储、任务队列 |
| 第 4～5 周 | 文件解析 | 上传、PDF、Excel、OCR、质量检查 |
| 第 6～7 周 | AI 抽取 | 字段、行项目、证据、置信度 |
| 第 8～9 周 | 匹配与异常 | 项目匹配、单位、异常检测 |
| 第 10 周 | 复核和对比 | 两阶段复核、比较表 |
| 第 11 周 | 导出和隐私 | Excel、PDF、删除、访客模式 |
| 第 12 周 | 封闭测试 | 修复、指标验证、小范围上线 |

如果只能投入业余时间，应按同样顺序延长周期，不要同时开发小程序。

---

## 18. 测试计划

### 18.1 单元测试

- 税费计算；
- 单位换算；
- 小计和总价；
- 币种格式；
- 文件哈希；
- 删除任务；
- 数据权限。

### 18.2 基准测试

每次模型、OCR 或提示词变更后运行：

- 关键字段准确率；
- 行项目召回率；
- 匹配准确率；
- 低置信度识别；
- 未提示严重错误；
- 成本和耗时。

### 18.3 集成测试

- 上传到导出完整链路；
- Worker 重试；
- 对象存储失效；
- 模型超时；
- OCR 失败；
- 支付回调；
- 自动删除。

### 18.4 安全测试

- 越权访问；
- 恶意文件；
- 超大文件；
- 文件名注入；
- 压缩炸弹；
- Prompt 注入文档；
- 公开分享链接猜测；
- 日志敏感信息泄漏。

### 18.5 视觉和可用性测试

- 桌面端 1366px；
- 宽屏；
- 平板查看；
- PDF 高亮；
- 大表格滚动；
- Excel 导出；
- A4 打印。

---

## 19. 发布质量指标

以下为目标，不是当前能力声明。

| 指标 | MVP 目标 |
|---|---:|
| 首次生成结果时间 | 5 分钟以内 |
| 未标低置信度的关键金额准确率 | ≥99.5% |
| 行项目匹配准确率 | ≥95% |
| 用户字段修正率 | <5% |
| 项目成功导出率 | ≥40% |
| 30 天重复使用率 | ≥25% |
| 用户处理时间节省 | ≥70% |
| 文件按期删除成功率 | 100% |

关键原则：

> 宁可多标记“需要确认”，也不能把错误数字显示为确定结果。

---

## 20. 上线后的继续/停止标准

公开测试后，满足以下条件才继续开发团队版：

- 20 位目标用户完成至少 50 个真实项目；
- 60% 以上项目无需客服即可导出；
- 至少 40% 的项目产生 Excel 或 PDF 导出；
- 至少 5 位用户在 30 天内使用 3 次；
- 至少 3 位用户完成真实付费；
- 没有发生未提示的严重金额错误；
- 至少 1 家企业愿意试用团队模板。

决策规则：

- 如果用户只需要 PDF 转 Excel：收缩为结构化抽取工具；
- 如果频率低但单次价值高：以按次付费为主；
- 如果重复使用明显：强化模板和历史规则；
- 如果团队共享需求强：进入团队版；
- 如果准确率无法达到要求：暂停商业化，继续完善证据和复核机制；
- 如果 SEO 流量不足：增加主动销售和企业试用，不批量生产低质量页面。

---

## 21. 主要风险

| 风险 | 应对方案 |
|---|---|
| AI 错误导致采购损失 | 原文证据、低置信度拦截、两次确认 |
| 通用模型复制功能 | 积累中文规则、模板、历史匹配和工作流 |
| 文件含商业秘密 | 加密、最短保存、不训练、立即删除 |
| 模型成本过高 | 原生解析优先、OCR 按需、缓存、分级模型 |
| 用户继续使用 Excel | 提供高质量 Excel 导出，不锁定用户 |
| 产品滑向采购 ERP | 严格限制在整理、比较和导出 |
| 文件格式长尾过多 | MVP 只支持四种格式 |
| 缺乏采购行业知识 | 建立用户顾问组，用真实样本驱动 |
| SEO 搜索量不足 | 同时提供按次付费和企业试用 |
| AI 合规要求变化 | 使用已备案模型，持续核对登记和标识要求 |

---

## 22. 参考资料

- [PriceMirror AI](https://www.pricemirror.ai/en)
- [PriceMirror AI 定价](https://www.pricemirror.ai/pricing)
- [Buyer24 免费报价比较工具](https://buyer24.ai/free-quote-comparison)
- [Buyer24 定价](https://buyer24.ai/pricing)
- [PDFExcel](https://pdfexcel.ai/)
- [Invoice Scraper](https://www.invoicescraper.com/)
- [国家互联网信息办公室：2025 年生成式人工智能服务已备案信息](https://www.cac.gov.cn/2026-01/09/c_1769688009588554.htm)
- [人工智能生成合成内容标识办法](https://www.nrta.gov.cn/art/2025/3/14/art_113_70340.html)

